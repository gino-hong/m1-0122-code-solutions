/* exported last */

// define a function named last with one parameter 'array'
// return the last value of the array

function last(array) {
  var i = array.length - 1;
  return array[i];
}

// define a function named last with one parameter 'array'
// define a variable i and assign it the value of the length of the array minus one
// return the array at index i
